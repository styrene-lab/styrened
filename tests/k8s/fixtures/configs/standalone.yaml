# Standalone mode configuration for styrened tests
# Used for isolated node testing

reticulum:
  mode: standalone
  transport_enabled: false
  announce_interval: 300

rpc:
  enabled: true

discovery:
  announce_interval: 300

chat:
  auto_reply_enabled: false
  auto_reply_message: "Test node - auto reply"
