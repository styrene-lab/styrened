# Styrene Mobile — Push Identity, APNs Distribution, and YubiKey Integration — Tasks

## 1. YubiKey on mobile is a session-unlock token, not a per-packet signer

- [x] 1.1 Implement YubiKey on mobile is a session-unlock token, not a per-packet signer

## 2. Android Keystore Ed25519 (Tier B) is the recommended mobile default on StrongBox devices

- [x] 2.1 Implement Android Keystore Ed25519 (Tier B) is the recommended mobile default on StrongBox devices

## 3. Push delivery path is determined by identity tier — no single universal push path

- [x] 3.1 Implement Push delivery path is determined by identity tier — no single universal push path
