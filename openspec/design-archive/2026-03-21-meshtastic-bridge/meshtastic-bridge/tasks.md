# Meshtastic Bridge Service — Tasks

## 1. Radio-only, no MQTT — local serial/BLE interface to Meshtastic hardware

- [x] 1.1 Implement Radio-only, no MQTT — local serial/BLE interface to Meshtastic hardware

## 2. Explicit bolt-on with hard boundary — no protocol convergence, no deep integration

- [x] 2.1 Implement Explicit bolt-on with hard boundary — no protocol convergence, no deep integration

## 3. Daemon service with optional meshtastic dependency, same toggle pattern as I2P/Yggdrasil

- [x] 3.1 Implement Daemon service with optional meshtastic dependency, same toggle pattern as I2P/Yggdrasil

## 4. No LXMF-to-Meshtastic translation — read-only ingest plus optional plaintext relay out

- [x] 4.1 Implement No LXMF-to-Meshtastic translation — read-only ingest plus optional plaintext relay out

## 5. Meshtastic nodes are permanently UNTRUSTED — no RBAC roles, no PQC, no RPC, no identity binding

- [x] 5.1 Implement Meshtastic nodes are permanently UNTRUSTED — no RBAC roles, no PQC, no RPC, no identity binding

## 6. Single [Mesh] transport label for all non-RNS LoRa mesh protocols

- [x] 6.1 Implement Single [Mesh] transport label for all non-RNS LoRa mesh protocols
