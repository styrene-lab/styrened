# Independent Cross-Enclave Features (Clipboard, Discovery, Jobs) — Tasks

## 1. Dual-path design: LXMF always, DirectLink when available, RBAC per-feature

- [x] 1.1 Implement Dual-path design: LXMF always, DirectLink when available, RBAC per-feature
