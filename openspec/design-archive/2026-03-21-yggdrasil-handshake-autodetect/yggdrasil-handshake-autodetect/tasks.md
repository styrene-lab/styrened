# Yggdrasil auto-detection in VPN handshake — Tasks

## 1. Ygg endpoint detection: admin socket first, interface scan fallback

- [x] 1.1 Implement Ygg endpoint detection: admin socket first, interface scan fallback

## 2. Ygg endpoint wins over clearnet in WG peer selection

- [x] 2.1 Implement Ygg endpoint wins over clearnet in WG peer selection

## 3. No YggdrasilInterfaceHelper service — RNS TCP config generation belongs in doctor --setup as opt-in output only

- [x] 3.1 Implement No YggdrasilInterfaceHelper service — RNS TCP config generation belongs in doctor --setup as opt-in output only

## 4. Failed Ygg endpoint: silent WG roaming fallback in v1; doctor diagnostic for persistent failures; no TUI alert unless >2 announce cycles unreachable

- [x] 4.1 Implement Failed Ygg endpoint: silent WG roaming fallback in v1; doctor diagnostic for persistent failures; no TUI alert unless >2 announce cycles unreachable
