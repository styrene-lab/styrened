# TUI Data State Model — Tasks

## 1. src/styrened/ui_state/ (new)

- [x] 1.1 Shared frontend-agnostic canonical state package sitting above IPC and below specific visual frontends. Houses node catalog, conversation index, config draft, local daemon state, and page-browser session state types plus normalization helpers.
- [x] 1.2 The shared state layer must not depend on Textual classes or screen/widget lifecycle hooks.
- [x] 1.3 IPC remains the authoritative daemon boundary; the shared state layer normalizes and projects but does not replace daemon truth.
- [x] 1.4 Identity-centric node normalization must happen once in shared state, not separately in each screen.
- [x] 1.5 Config editing must use typed draft state with explicit persisted/editable/save/reset semantics rather than raw dict mutation in visual layers.
- [x] 1.6 Builder functions should accept explicit input snapshots rather than reaching back into IPC clients or services.
- [x] 1.7 NodeRecord remains identity-centric and composes route, overlay, relationship, and authority submodels instead of flattening all concerns.
- [x] 1.8 Ephemeral connection state belongs in sessions.py so node models do not absorb relay/terminal/direct-link/VPN runtime details.
- [x] 1.9 Shared state modules should expose deterministic pure transforms suitable for unit tests and reuse by future non-TUI frontends.
- [x] 1.10 MailIndexState currently covers direct/group/forum scope typing and normalization, but scope-aware screen routing beyond direct-thread compatibility remains unfinished.
- [x] 1.11 Inbox/Mail still relies on some legacy conversation metadata such as attachment counts until the canonical mail state surface is expanded further.
- [x] 1.12 Proposed package layout: `src/styrened/ui_state/{base,nodes,sessions,daemon,config,conversations,page_browser}.py`. The first implementation wave should define canonical dataclasses plus pure builder functions that accept authoritative IPC payload snapshots and return normalized state aggregates. Builders remain frontend-agnostic and avoid Textual dependencies; frontend layers may add thin projection adapters on top.
- [x] 1.13 The shared `ui_state` layer sits between authoritative daemon IPC snapshots and any visual frontend. It is a pure normalization layer: no Textual dependencies, no IPC clients, no database access, no background tasks, and no shadow persistence. Its public surface consists of typed canonical aggregates, focused submodels, explicit input bundles, and pure builder functions. Frontends may keep in-memory current/previous snapshots for presentation, but long-lived state tracking remains with daemon capability owners.
- [x] 1.14 Every capability-specific submodel must be zero-cost when the corresponding daemon capability is absent. Stable schema presence is acceptable, but builders only populate capability state from authoritative inputs. Disabled capabilities remain unsupported or absent; they do not cause `ui_state` to create synthetic peers, runtime trackers, pollers, or persistence. Yggdrasil should therefore already exist in the shared interface as optional overlay/runtime submodels, but it must remain unpopulated unless daemon inputs provide Yggdrasil data.
- [x] 1.15 First-pass shared interfaces:
- [x] 1.16 `SettingsScreen` now uses the shared `ConfigDraftState` contract operationally in its save path: it snapshots persisted core config, builds an editable draft from current form inputs, validates that draft before mutating the live `StyreneConfig`, and updates shared draft state for saving/validation/save-error/save-success semantics. This keeps persisted-vs-editable semantics explicit while preserving the historical behavior that the caller's original config object reflects successful saves.
- [x] 1.17 The NodeInfoPanel cleanup clarified an important application point for the shared state model: dashboard-level canonical snapshots should be constructed outside the widget and pushed into it, rather than letting the widget call the bridge directly. For this slice, identity and mesh-count are the first candidate fields. Near-term migration can use the existing `_apply_identity_snapshot()` and `_apply_mesh_catalog_count()` seams as compatibility adapters while DashboardScreen or a dedicated dashboard adapter begins owning snapshot construction.
- [x] 1.18 The first concrete panel-scoped snapshot is now implemented as `HomeNodeInfoState` in `ui_state.daemon`. DashboardScreen computes normalized mesh count, builds the panel snapshot with `build_home_node_info_state(...)`, and hands that object to `NodeInfoPanel.apply_home_snapshot(...)`. This replaces multi-field direct mutation as the primary composition path for the Home summary panel while keeping a narrow, panel-focused contract instead of introducing a giant dashboard-wide state object.
- [x] 1.19 The Home panel snapshot now covers the adjacent comms summary as well as daemon/runtime status. `build_home_node_info_state(...)` accepts authoritative conversation/contact/auto-reply payloads and derives unread count, conversation count, contact count, message totals, and auto-reply state. DashboardScreen now reapplies NodeInfoPanel from a coherent panel snapshot after those payloads resolve, instead of mutating the comms presentation fields directly. This keeps the Home summary contract panel-scoped while widening it just enough to cover the full rendered surface.
- [x] 1.20 The remaining mixed-local responsibility in NodeInfoPanel has been reduced by adding a second panel-scoped snapshot: `HomeNodeLocalState`. DashboardScreen now gathers local hardware/config inputs (system info, primary interface, removable media count, mode, identity presentation/provider) and pushes them into the widget via `apply_home_local_snapshot(...)`. This removes widget-owned local refresh in IPC-managed Home and leaves NodeInfoPanel closer to a pure presentation role, with only legacy/non-IPC local refresh paths still using the old internal loaders.
- [x] 1.21 The Home snapshot boundary is no longer just an IPC-managed path. NodeInfoPanel's non-IPC fallback/local mode now constructs and applies `HomeNodeLocalState` through `_build_local_fallback_snapshot()` before handling legacy Reticulum reads. This keeps the local compatibility path aligned with the panel-scoped snapshot decision and reduces the number of code paths that mutate Home presentation fields directly.
- [x] 1.22 The local compatibility path is now explicitly adapter-shaped. `NodeInfoPanel` delegates non-IPC local reads to a private fallback builder helper that returns `HomeNodeLocalState`, then applies that snapshot through the same widget contract used by IPC-managed Home. This means the panel now consistently consumes panel-scoped local snapshots in both modes, with the fallback loader acting as an implementation adapter rather than a second competing state model.
- [x] 1.23 Audit date: 2026-03-10. Scope: mesh_device_detail.py, exploration.py, dashboard.py, contacts.py, chat_widget.py, conversation_service.py, ipc/client.py, models/mesh_device.py, ui_state/nodes.py.

## 2. src/styrened/tui/ (modified)

- [x] 2.1 Consumes shared canonical state via thin Textual-specific projection layers and view models rather than raw IPC payload shaping.
- [x] 2.2 Proposed package layout: `src/styrened/ui_state/{base,nodes,sessions,daemon,config,conversations,page_browser}.py`. The first implementation wave should define canonical dataclasses plus pure builder functions that accept authoritative IPC payload snapshots and return normalized state aggregates. Builders remain frontend-agnostic and avoid Textual dependencies; frontend layers may add thin projection adapters on top.
- [x] 2.3 The shared `ui_state` layer sits between authoritative daemon IPC snapshots and any visual frontend. It is a pure normalization layer: no Textual dependencies, no IPC clients, no database access, no background tasks, and no shadow persistence. Its public surface consists of typed canonical aggregates, focused submodels, explicit input bundles, and pure builder functions. Frontends may keep in-memory current/previous snapshots for presentation, but long-lived state tracking remains with daemon capability owners.
- [x] 2.4 Every capability-specific submodel must be zero-cost when the corresponding daemon capability is absent. Stable schema presence is acceptable, but builders only populate capability state from authoritative inputs. Disabled capabilities remain unsupported or absent; they do not cause `ui_state` to create synthetic peers, runtime trackers, pollers, or persistence. Yggdrasil should therefore already exist in the shared interface as optional overlay/runtime submodels, but it must remain unpopulated unless daemon inputs provide Yggdrasil data.
- [x] 2.5 First-pass shared interfaces:
- [x] 2.6 `SettingsScreen` now uses the shared `ConfigDraftState` contract operationally in its save path: it snapshots persisted core config, builds an editable draft from current form inputs, validates that draft before mutating the live `StyreneConfig`, and updates shared draft state for saving/validation/save-error/save-success semantics. This keeps persisted-vs-editable semantics explicit while preserving the historical behavior that the caller's original config object reflects successful saves.
- [x] 2.7 The NodeInfoPanel cleanup clarified an important application point for the shared state model: dashboard-level canonical snapshots should be constructed outside the widget and pushed into it, rather than letting the widget call the bridge directly. For this slice, identity and mesh-count are the first candidate fields. Near-term migration can use the existing `_apply_identity_snapshot()` and `_apply_mesh_catalog_count()` seams as compatibility adapters while DashboardScreen or a dedicated dashboard adapter begins owning snapshot construction.
- [x] 2.8 The first concrete panel-scoped snapshot is now implemented as `HomeNodeInfoState` in `ui_state.daemon`. DashboardScreen computes normalized mesh count, builds the panel snapshot with `build_home_node_info_state(...)`, and hands that object to `NodeInfoPanel.apply_home_snapshot(...)`. This replaces multi-field direct mutation as the primary composition path for the Home summary panel while keeping a narrow, panel-focused contract instead of introducing a giant dashboard-wide state object.
- [x] 2.9 The Home panel snapshot now covers the adjacent comms summary as well as daemon/runtime status. `build_home_node_info_state(...)` accepts authoritative conversation/contact/auto-reply payloads and derives unread count, conversation count, contact count, message totals, and auto-reply state. DashboardScreen now reapplies NodeInfoPanel from a coherent panel snapshot after those payloads resolve, instead of mutating the comms presentation fields directly. This keeps the Home summary contract panel-scoped while widening it just enough to cover the full rendered surface.
- [x] 2.10 The remaining mixed-local responsibility in NodeInfoPanel has been reduced by adding a second panel-scoped snapshot: `HomeNodeLocalState`. DashboardScreen now gathers local hardware/config inputs (system info, primary interface, removable media count, mode, identity presentation/provider) and pushes them into the widget via `apply_home_local_snapshot(...)`. This removes widget-owned local refresh in IPC-managed Home and leaves NodeInfoPanel closer to a pure presentation role, with only legacy/non-IPC local refresh paths still using the old internal loaders.
- [x] 2.11 The Home snapshot boundary is no longer just an IPC-managed path. NodeInfoPanel's non-IPC fallback/local mode now constructs and applies `HomeNodeLocalState` through `_build_local_fallback_snapshot()` before handling legacy Reticulum reads. This keeps the local compatibility path aligned with the panel-scoped snapshot decision and reduces the number of code paths that mutate Home presentation fields directly.
- [x] 2.12 The local compatibility path is now explicitly adapter-shaped. `NodeInfoPanel` delegates non-IPC local reads to a private fallback builder helper that returns `HomeNodeLocalState`, then applies that snapshot through the same widget contract used by IPC-managed Home. This means the panel now consistently consumes panel-scoped local snapshots in both modes, with the fallback loader acting as an implementation adapter rather than a second competing state model.
- [x] 2.13 Audit date: 2026-03-10. Scope: mesh_device_detail.py, exploration.py, dashboard.py, contacts.py, chat_widget.py, conversation_service.py, ipc/client.py, models/mesh_device.py, ui_state/nodes.py.

## 3. openspec/changes/tui-structural-refactor/ (modified)

- [x] 3.1 OpenSpec change extended to capture shared UI-facing state model requirements and tasks so the refactor does not remain TUI-only in design.
- [x] 3.2 Proposed package layout: `src/styrened/ui_state/{base,nodes,sessions,daemon,config,conversations,page_browser}.py`. The first implementation wave should define canonical dataclasses plus pure builder functions that accept authoritative IPC payload snapshots and return normalized state aggregates. Builders remain frontend-agnostic and avoid Textual dependencies; frontend layers may add thin projection adapters on top.
- [x] 3.3 The shared `ui_state` layer sits between authoritative daemon IPC snapshots and any visual frontend. It is a pure normalization layer: no Textual dependencies, no IPC clients, no database access, no background tasks, and no shadow persistence. Its public surface consists of typed canonical aggregates, focused submodels, explicit input bundles, and pure builder functions. Frontends may keep in-memory current/previous snapshots for presentation, but long-lived state tracking remains with daemon capability owners.
- [x] 3.4 Every capability-specific submodel must be zero-cost when the corresponding daemon capability is absent. Stable schema presence is acceptable, but builders only populate capability state from authoritative inputs. Disabled capabilities remain unsupported or absent; they do not cause `ui_state` to create synthetic peers, runtime trackers, pollers, or persistence. Yggdrasil should therefore already exist in the shared interface as optional overlay/runtime submodels, but it must remain unpopulated unless daemon inputs provide Yggdrasil data.
- [x] 3.5 First-pass shared interfaces:
- [x] 3.6 `SettingsScreen` now uses the shared `ConfigDraftState` contract operationally in its save path: it snapshots persisted core config, builds an editable draft from current form inputs, validates that draft before mutating the live `StyreneConfig`, and updates shared draft state for saving/validation/save-error/save-success semantics. This keeps persisted-vs-editable semantics explicit while preserving the historical behavior that the caller's original config object reflects successful saves.
- [x] 3.7 The NodeInfoPanel cleanup clarified an important application point for the shared state model: dashboard-level canonical snapshots should be constructed outside the widget and pushed into it, rather than letting the widget call the bridge directly. For this slice, identity and mesh-count are the first candidate fields. Near-term migration can use the existing `_apply_identity_snapshot()` and `_apply_mesh_catalog_count()` seams as compatibility adapters while DashboardScreen or a dedicated dashboard adapter begins owning snapshot construction.
- [x] 3.8 The first concrete panel-scoped snapshot is now implemented as `HomeNodeInfoState` in `ui_state.daemon`. DashboardScreen computes normalized mesh count, builds the panel snapshot with `build_home_node_info_state(...)`, and hands that object to `NodeInfoPanel.apply_home_snapshot(...)`. This replaces multi-field direct mutation as the primary composition path for the Home summary panel while keeping a narrow, panel-focused contract instead of introducing a giant dashboard-wide state object.
- [x] 3.9 The Home panel snapshot now covers the adjacent comms summary as well as daemon/runtime status. `build_home_node_info_state(...)` accepts authoritative conversation/contact/auto-reply payloads and derives unread count, conversation count, contact count, message totals, and auto-reply state. DashboardScreen now reapplies NodeInfoPanel from a coherent panel snapshot after those payloads resolve, instead of mutating the comms presentation fields directly. This keeps the Home summary contract panel-scoped while widening it just enough to cover the full rendered surface.
- [x] 3.10 The remaining mixed-local responsibility in NodeInfoPanel has been reduced by adding a second panel-scoped snapshot: `HomeNodeLocalState`. DashboardScreen now gathers local hardware/config inputs (system info, primary interface, removable media count, mode, identity presentation/provider) and pushes them into the widget via `apply_home_local_snapshot(...)`. This removes widget-owned local refresh in IPC-managed Home and leaves NodeInfoPanel closer to a pure presentation role, with only legacy/non-IPC local refresh paths still using the old internal loaders.
- [x] 3.11 The Home snapshot boundary is no longer just an IPC-managed path. NodeInfoPanel's non-IPC fallback/local mode now constructs and applies `HomeNodeLocalState` through `_build_local_fallback_snapshot()` before handling legacy Reticulum reads. This keeps the local compatibility path aligned with the panel-scoped snapshot decision and reduces the number of code paths that mutate Home presentation fields directly.
- [x] 3.12 The local compatibility path is now explicitly adapter-shaped. `NodeInfoPanel` delegates non-IPC local reads to a private fallback builder helper that returns `HomeNodeLocalState`, then applies that snapshot through the same widget contract used by IPC-managed Home. This means the panel now consistently consumes panel-scoped local snapshots in both modes, with the fallback loader acting as an implementation adapter rather than a second competing state model.
- [x] 3.13 Audit date: 2026-03-10. Scope: mesh_device_detail.py, exploration.py, dashboard.py, contacts.py, chat_widget.py, conversation_service.py, ipc/client.py, models/mesh_device.py, ui_state/nodes.py.

## 4. src/styrened/ui_state/base.py (new)

- [x] 4.1 Shared load/degradation metadata, issue reporting, provenance, and common enums used by all canonical state aggregates.

## 5. src/styrened/ui_state/nodes.py (new)

- [x] 5.1 Identity-centric node catalog models, route aspects, overlay state, peer relationship state, authority metadata, and normalization/build functions.

## 6. src/styrened/ui_state/sessions.py (new)

- [x] 6.1 Ephemeral connection/session models for direct links, relay sessions, terminals, VPN handshakes, and other edge-like live state that should not be flattened into NodeRecord.

## 7. src/styrened/ui_state/daemon.py (new)

- [x] 7.1 Canonical local daemon state aggregate combining daemon status, identity, hub status, and overlay runtime summaries.

## 8. src/styrened/ui_state/config.py (new)

- [x] 8.1 Persisted snapshot versus editable draft state, dirty-field tracking, validation results, and save/reset semantics for UI configuration editors.

## 9. src/styrened/ui_state/mail.py (new)

- [x] 9.1 Canonical async Mail state aggregate with conversation scope kinds, thread/message records, group/forum metadata, and pure mail-index normalization builder.

## 10. src/styrened/tui/screens/inbox.py (modified)

- [x] 10.1 Inbox is now acting as the initial Mail workspace compatibility screen and consumes canonical MailIndexState instead of rendering raw conversation dicts directly.

## 11. src/styrened/tui/app.py (modified)

- [x] 11.1 App-level navigation now includes Mail-first language and action_open_mail(), with action_open_inbox() preserved as a compatibility alias during migration.
