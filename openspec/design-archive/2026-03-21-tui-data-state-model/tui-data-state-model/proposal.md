# TUI Data State Model

## Intent

Define the canonical typed state model that sits immediately behind the TUI. Focus on how daemon/IPC data is normalized, merged, refreshed, and projected into screen-specific view models so presentation logic becomes thin and consistent.
