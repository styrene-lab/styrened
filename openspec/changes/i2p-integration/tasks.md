# I2P Integration — Tasks

## 1. RNS over I2P SAM deferred indefinitely

- [ ] 1.1 Implement RNS over I2P SAM deferred indefinitely

## 2. styrened detects i2pd but never manages it — follows BATMAN-ADV model not WireGuard model

- [ ] 2.1 Implement styrened detects i2pd but never manages it — follows BATMAN-ADV model not WireGuard model

## 3. I2P eepsite browsing via HTTP proxy — PageCacheService becomes transport-agnostic

- [ ] 3.1 Implement I2P eepsite browsing via HTTP proxy — PageCacheService becomes transport-agnostic

## 4. CAPABILITY_I2P bit in announces — .b32.i2p address fetched via DirectLink /meta

- [ ] 4.1 Implement CAPABILITY_I2P bit in announces — .b32.i2p address fetched via DirectLink /meta

## 5. i2pd follows DaemonMode: DISABLED | ADOPT | MANAGED — not detect-only

- [ ] 5.1 Implement i2pd follows DaemonMode: DISABLED | ADOPT | MANAGED — not detect-only

## 6. b32 address detection: I2PControl API (autodetect) with fallback to operator config

- [ ] 6.1 Implement b32 address detection: I2PControl API (autodetect) with fallback to operator config

## 7. Page browser I2P fetch requires explicit i2p.mode != disabled — never opportunistic

- [ ] 7.1 Implement Page browser I2P fetch requires explicit i2p.mode != disabled — never opportunistic
